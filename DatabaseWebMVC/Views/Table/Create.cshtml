@model DatabaseWebMVC.Models.TableViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>@Model.tableName</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create", asp-controller="Table">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input asp-for="dbName" value="@Model.dbName" class="form-control" type="hidden"/>
                <span asp-validation-for="dbName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="tableName" class="control-label"></label>
                <input asp-for="tableName" class="form-control" />
                <span asp-validation-for="tableName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    @Html.ActionLink("Back to tables", "Index", "Table", new {dbName = Model.dbName})
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
